export enum UserRole {
  SUPER_ADMIN = "SUPER_ADMIN",
  SECRETARY = "SECRETARY",
  INSTRUCTOR = "INSTRUCTOR",
}

export enum TransmissionType {
  MANUAL = "MANUAL",
  AUTOMATIC = "AUTOMATIC",
  BOTH = "BOTH",
}